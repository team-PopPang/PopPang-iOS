import SwiftUI

public struct {{ name }}FeatureRootView: View {
    public init() {}

    public var body: some View {
        {{ name }}FeatureView(
            store: {{ name }}FeatureStore()
        )
    }
}
