import ProjectDescription

let project = Project(
    name: "{{ name }}Domain",
    targets: [
        .target(
            name: "{{ name }}Domain",
            destinations: .iOS,
            product: .framework,
            bundleId: "{{ bundleIdPrefix }}.domain.{{ name | lowercase }}",
            deploymentTargets: .iOS("{{ deploymentTarget }}"),
            infoPlist: .default,
            sources: ["Sources/**"],
            dependencies: []
        ),
        .target(
            name: "{{ name }}DomainTests",
            destinations: .iOS,
            product: .unitTests,
            bundleId: "{{ bundleIdPrefix }}.domain.{{ name | lowercase }}.tests",
            deploymentTargets: .iOS("{{ deploymentTarget }}"),
            infoPlist: .default,
            sources: ["Tests/**"],
            dependencies: [.target(name: "{{ name }}Domain")]
        ),
    ]
)
